#向服务注册中心注册
eureka:
  client:
    service-url:
      defaultZone: http://user:pwd@peer1:8761/eureka/
#设置config client
spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      uri: http://localhost:8091
      label: master

              